# Core dependencies
python-dotenv==1.0.0        # Load environment variables for API keys, configurations
PyYAML==6.0.1              # Parse and manage configuration files

# Data Processing
tiktoken==0.5.2            # Tokenizer for handling text chunking
unstructured==0.11.4       # Extract content from various file types (e.g., PDF, docx)

# Embeddings & Vector Database
openai==1.3.9              # API for calling OpenAI embeddings (or modify for other providers)
chromadb==0.4.22           # Lightweight vector database for embedding storage
faiss-cpu==1.7.4           # Efficient vector search (alternative to ChromaDB)
sentence-transformers==2.2.2  # Alternative embedding models if not using OpenAI API

# Query Processing & Retrieval
langchain==0.1.13          # Framework for chaining LLM-based retrieval workflows
duckduckgo-search==3.9.9   # Optional: Web search capability for expanded retrieval
pydantic==2.5.3            # Data validation and schema definition for structured inputs

# FastAPI & Web API (For Future UI)
fastapi==0.109.2           # Web framework for building the API
uvicorn==0.27.1            # ASGI server for FastAPI
jinja2==3.1.3              # Templating engine if needed for UI rendering

# Terminal Interaction
rich==13.7.1               # Better console UI with colored output
click==8.1.7               # Command-line argument handling

# Testing & Validation
pytest==8.0.0              # Unit testing framework
pytest-asyncio==0.23.6     # Support for async testing
mypy==1.8.0                # Static type checking
black==23.12.1             # Code formatting for consistency
isort==5.13.2              # Import sorting for clean code

# Caching & Performance Optimization
diskcache==5.6.3           # Local caching to speed up repeated queries
joblib==1.3.2              # Parallel processing for embedding computations

# Compatibility Fixes for macOS (If Needed)
numpy==1.26.3              # Numerical computations (required by FAISS)
scipy==1.11.4              # Scientific computing, needed for FAISS